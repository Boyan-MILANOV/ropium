#!/usr/bin/env python3
#import cProfile, pstats
import ropgenerator.main.Main as Main

#pr = cProfile.Profile()
#pr.enable()
Main.main()
#pr.disable()
#ps = pstats.Stats(pr).sort_stats('cumulative')
#ps.print_stats(20)

